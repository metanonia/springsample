<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.metanonia.springsample.mapper.CandleMapper">
    <select id="findFirst10Candle" resultType="HashMap">
        SELECT * FROM candle ORDER BY ts LIMIT 10
    </select>
</mapper>